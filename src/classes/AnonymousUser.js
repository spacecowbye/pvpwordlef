/**
 * Represents an anonymous user in the system.
 */
class AnonymousUser {

    /**
     * Creates a new anonymous user.
     * @param {string} user_id - Unique identifier for the anonymous user.
     */
    constructor(user_id) {
        this.user_id = user_id;
    }
}

